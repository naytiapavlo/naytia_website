# 05 · 阶段计划与任务清单

更新日期：2026-09-16  
状态：仅规划。阶段工期在确认内容量、首批工具及部署约束后估算，不预设虚假的完成日期。

## 阶段 0：规划与边界

- [x] 建立 `docs/plans/` 专用规划目录与文档索引。
- [x] 记录顶级导航、博客位置与白色像素视觉方向（初始为三项导航，2026-09-16 增加论坛为四项）。
- [x] 提出模块边界、内容模型、工具协议及验收标准。
- [x] 2026-09-16：按用户要求将论坛模块与账号系统（作用于论坛与工具箱）纳入产品范围，并在静态 Demo 中交付演示实现。
- [ ] 确定正式头像、简介、站名与公开联系方式。
- [ ] 确定首发内容和两个工具的实际优先级。
- [ ] 确定部署平台、域名、成本范围与主要访问地区。
- [ ] 将正式技术选型写入 ADR。

交付物：本目录文档、内容清单、首批工具需求说明、已接受的技术 ADR。未确定的素材与平台不阻止先实现本地基础层。

## 阶段 1：工程基础与设计系统

依赖：阶段 0 的方向与本地技术选择。

- [x] 备份并核对现有 Demo：已复制到 `prototypes/pixel-demo/`（SHA-256 校验一致，2026-09-16）。
- [x] 初始化正式工程（2026-09-16）：Astro 5 + TypeScript strict 前端、FastAPI + SQLite 后端骨架；`npm run build` / `astro check`（0 错误）与后端 pytest（17 项）通过。
- [x] 建立设计 tokens、像素图标、基础组件、公共布局和四项导航（`src/styles/`、`BaseLayout`、`components/ui/`）。
- [x] 将头像、简介、导航与首页内容排序移入站点配置（`src/config/site.ts`）。
- [x] 建立真实路径路由、404 与旧入口迁移：`/` `/blog/` `/works/` `/tools/` `/forum/` `/404`；旧 Demo 的 `#home/#works/#tools` hash 视图由 `prototypes/` 存档替代，不映射。
- [x] 建立公开资源边界：`ancient_debris/`、`backend/data/`、`dist/`、环境变量文件均不入库。

验收结果（2026-09-16）：本地生产构建可运行（6 页）；布局沿用演示版移动端断点；头像、主色、导航均由 tokens/配置驱动，无需编辑页面模板。

验收：本地生产构建可运行；手机与桌面基础布局正常；更换头像与主色不需要编辑每个页面。

## 阶段 2：博客与作品

依赖：阶段 1；可用真实内容逐步替换内部样例。

- [ ] 定义 schema、稳定 ID、slug、发布状态与引用关系。
- [ ] 实现文件内容适配器及发布状态过滤。
- [ ] 完成首页、博客列表/详情与作品列表/详情。
- [ ] 完成分类、标签与分页的最小实现。
- [ ] 完成视频链接、作者与来源、兼容性验证记录和资源清单。
- [ ] 完成正文目录、代码块、SEO、RSS 与 sitemap。
- [ ] 校验草稿隔离、内容关联、资源引用与 slug 冲突。

验收：新增文章或作品只需添加内容和资源清单；修改标题不破坏既有链接；博客与作品能关联到未来工具。

## 阶段 3：工具宿主与第一批工具

依赖：阶段 1 的路由与 UI、阶段 2 的关联内容契约。

- [x] 建立 manifest、注册表、详情加载与统一状态显示（2026-09-16，见 06 开发日志）。
- [x] 落地输入输出校验、规则集、结果/错误协议。
- [x] ~~实现区块坐标助手与独立对照测试。~~ **已于 2026-09-16 按需求移除**（连同专属测试）。
- [x] ~~实现材料清单助手，处理不同堆叠上限与容器容量。~~ **已于 2026-09-16 按需求移除**。
- [x] 实现每个工具的用途、示例、版本、限制与结果呈现。〔示例入口保留未启用〕
- [x] 校验按需加载、独立 URL 与失败隔离：各工具独立 chunk，`/tools/` 与首页不加载工具实现。
- [x] 工具与账号联动：登录后可收藏，未登录保持只读并可浏览（阶段 4 提前完成的部分）。
- [ ] 仅在实际需要时加入 Worker 执行器及取消协议。
- [x] 第三个工具的扩展演练（02 文档第 9 节，2026-09-16）：新增 `.mcstructure` 编辑器。
  演练当时**前两个工具的目录文件与实现零改动**；只动了注册表两处登记（`registry.ts` 加一行懒加载、
  `manifests.ts` 加一项）与详情页样式引用一处，正是 02 文档第 9 节允许的范围。
  证据见 [`08-开发日志-mcstructure编辑器.md`](08-开发日志-mcstructure编辑器.md)。
- [x] **工具箱收敛为单一工具（2026-09-16）**：按需求移除区块坐标助手与材料清单助手，
  当前工具为 `.mcstructure` 编辑器。历史实现见 git 提交 `fa47d7b`。

验收（原定）：两项工具真实可用；新增下一项工具主要修改新目录与注册表；其他工具不需要配合修改。

验收结果（2026-09-16，当时）：区块坐标助手与材料清单助手均可用，`status='stable'`；`npm test` 16 项引擎对照测试通过、`npm run check` 0 错误、`npm run build` 8 页；无头浏览器实测计算、非法输入提示、登录门槛与收藏闭环均通过。证据与已知限制见 [`06-开发日志-工具系统实装.md`](06-开发日志-工具系统实装.md)。

验收结果（2026-09-16，收敛后）：工具箱只有 `.mcstructure` 编辑器，`status='stable'`；
`npm test` 56 项通过（结构编辑器 27 + SNBT 29）、`pytest` 138 项通过、
`npm run check` 0 错误、`npm run build` 8 页（含 `/tools/mcstructure-editor/`）；
真实无头浏览器端到端 48 项断言通过。证据见 [`08-开发日志`](08-开发日志-mcstructure编辑器.md)。

> 注意：阶段 3 原有的「两项工具」验收标准已因这次移除而失效（首版完成定义里的
> 「两项工具」同样需要按实际工具数重新定义）。

验收结果（2026-09-16，第三个工具）：`.mcstructure` 编辑器可用（`status='stable'`）；`npm test` 43 项通过（工具引擎 16 + 结构编辑器 27）、`npm run check` 0 错误、`npm run build` 10 页；真实无头浏览器端到端（上传真实结构文件 → 编辑 → 撤销 → 下载 → 离线复解析）通过。证据见 [`08-开发日志-mcstructure编辑器.md`](08-开发日志-mcstructure编辑器.md)。

## 阶段 4：论坛与账号系统

依赖：阶段 1 的路由与 UI。演示版不依赖部署决策，可先行；服务端语言已定 Python（ADR-001），数据库与托管依赖 D07 余项。

- [x] 演示版（2026-09-16，静态 Demo 内实现）：论坛视图（版块筛选、帖子列表、详情、发帖、回复）、注册登录（本地存储 + 本地散列 + 会话保持）、工具箱登录门槛与「收藏常用工具」（按账号隔离）、演示数据重置入口；界面明示演示边界。
- [x] 后端语言决策（2026-09-16）：定为 Python，建议基线 FastAPI，契约以 OpenAPI 为单一事实源，已写入 ADR-001。
- [x] 后端骨架（2026-09-16，同日完成）：FastAPI 应用工厂、SQLAlchemy 模型（Account/AuthSession/ForumThread/ForumReply/ToolFavorite）、argon2id 密码哈希、keyset 分页、结构化错误；pytest 17 项通过（认证流程、论坛权限、收藏隔离）。
- [x] 角色体系与动态主页（2026-09-16，ADR-002）：member/admin/superadmin 三级角色（首账号 bootstrap + 邀请码）、超管在线编辑主页（昵称/简介/头像/GIF 等动图背景 + 可调遮罩）、`GET/PUT /api/site-config`（公开读/超管写、覆盖项浅合并、http(s) 协议校验）、管理员审核删帖、角色管理 API；models/routers 按模块拆分支持并行开发；pytest 28 项通过，浏览器联调通过。
- [ ] 固化契约：`ForumThread` / `ForumReply` / `Account` 模型与 `ThreadRepository` / `SessionProvider` 接口（见 03 文档），并打通 OpenAPI → 前端 TS 类型生成（`npm run api:types` 脚本已备）。
- [x] 数据库决策（2026-09-16）：定为 SQLite，经 SQLAlchemy 访问并保留 Postgres 迁移路径（ADR-001 已更新）。
- [ ] 确定托管平台与部署方式，补全 D07。
- [ ] 服务端账号（Python/FastAPI 基线）：注册、登录、会话管理、argon2id 密码哈希、基础限流。〔除限流外已完成〕
- [x] 论坛前端接入真实 API（2026-09-16）：`/forum/` 从「建设中」占位改为静态外壳 + 客户端调用真实 API，
  列表（版块筛选、游标分页、结构徽标）、详情、发帖、回复、删除全部可用；hash 路由
  （`#/t/12`、`#/new`）让前进/后退、刷新与分享链接都能用。实装与验证见
  [`10-开发日志`](10-开发日志-论坛结构附件与3D预览.md)。
- [x] 论坛读写 API 与权限：未登录只读；登录后发帖/回复；作者可删除自有内容；正文转义渲染
  （`escapeHtml` + 只把裸链接变成可点链接，不渲染 Markdown——用户输入不走可信作者流程）。
- [x] **论坛进阶能力 · 附件（2026-09-16 提前落地）**：发帖可随帖上传 ≤10 MB 的
  `.mcstructure`，由内置解析器解析出材料清单，并生成轻量载荷供网页端做 3D 预览；
  附件可下载。随后追加**封面图**（≤5 MB，PNG/JPEG/GIF/WebP）：列表显示 16:9 缩略图、
  详情页显示原图，作者与管理员可删除；封面类型**只认文件头字节**（拒绝 SVG），
  提供时带 `nosniff` 与 CSP。
  决策见 [`ADR-006`](decisions/ADR-006-论坛结构附件与网页3D预览.md)。
  已知限制：无存储配额与限流、颜色为示意色、超大结构会抽稀、封面不做服务端压缩。
- [x] 工具箱联动（2026-09-16）：收藏绑定账号、按账号隔离、未登录只读并可浏览；多设备同步作为后续独立任务。最近使用仍待实现（见 06 开发日志第 5 节）。

验收：未登录可浏览论坛与工具箱，但不能发帖/回复/收藏；登录后上述功能可用且刷新后保持；示例帖子带「示例」标识；演示数据清理（重置）入口可用。

验收结果（2026-09-16，论坛前端接入真实 API + 附件）：`/forum/` 已从占位改为真实可用——
列表/详情/发帖/回复/删除全部走服务端 API，未登录只读、登录后可写；发帖可带结构文件与封面。
`npm test` 115 项通过（3D 渲染核心与封面展示 59 项）、`pytest` 303 项通过、0 失败
（论坛附件新增 35 + 37 项）、`astro check` 0 error、`npm run build` 10 页；
真实无头浏览器端到端 62 项断言通过（上传真实结构文件与封面 → 材料清单 → 3D 预览 →
分层/旋转 → 下载 → 列表缩略图 → 删封面 → 回复 → 刷新复现）。
本轮验证顺带发现并修掉五个缺陷：版块接口不可用时的空下拉框、时间戳丢失 UTC 标记、
五项导航折行、封面被拒时结构文件已落盘的顺序问题、空文件报错指向错误。
证据见 [`10-开发日志`](10-开发日志-论坛结构附件与3D预览.md)。

## 阶段 5：首版发布

依赖：阶段 2/3/4 完成（论坛与账号至少演示版可用），部署配置与正式内容就绪。

- [ ] 按工程文档执行内容、算法、关键路径与可访问性检查。
- [ ] 验证直达链接、搜索引擎元数据、资源下载和移动端阅读。
- [ ] 清除生产产物中的演示文章与未确认占位素材。
- [ ] 论坛与账号的演示边界说明、数据重置入口与正式服务切换方案已记录。
- [x] 配置托管与域名，验证预览环境后发布。
      **本机侧已实装并验证（2026-09-16）**：本地同源入口 `deploy/server.py`
      （静态产物 + `/api` 反代，127.0.0.1:8080）、启动脚本、`SITE_URL` 构建期配置；
      29 个站内 URL 全通、会话 Cookie 往返、multipart 上传、真实 IP 传递、
      目录穿越与路径前缀误伤均有用例钉住（`python deploy\test_server.py` 21 项）。
      **待办（公网侧）**：樱花 HTTP 隧道 + GoDaddy 解析需在面板/DNS 中配置，
      之后按 [`14-部署手册`](14-部署手册-域名与内网穿透.md) 第 6 节的公网清单
      用手机流量验收（证书、直达链接、https 下登录态、移动端）。
      决策见 [`ADR-009`](decisions/ADR-009-同源单端口部署与内网穿透.md)。
- [ ] 记录备份与回退方式，保存稳定版本。
      （回退方式已写在 ADR-009「数据与回退」；SQLite 与附件目录的定期备份仍未做。）

验收：一个可分享的正式网站，包含真实博客、作品和**可运行工具**（原定「至少两个」，
2026-09-16 工具箱收敛为单一工具后改为按实际工具数验收）；论坛与账号满足阶段 4 验收；
具体发布动作在后续实施任务中执行。

## 阶段 6：按需求持续扩展

| 候选功能 | 启动条件 | 独立交付边界 |
| --- | --- | --- |
| 时序计算工具 | 规则和使用场景明确 | 新工具模块 |
| 跨站内内容搜索 | 标签与列表不足以快速查找 | 索引生成器与搜索适配器 |
| 在线内容后台 | 文件编辑频率/成本明显增加 | CMS 适配器、身份与发布流程 |
| 复杂 MC 分析 | 有可验证算法、样例与性能需求 | 专项引擎、Worker/WASM、测试数据 |
| 文件导入导出 | 工具确实需要交换结果 | 格式协议、迁移与限制 |
| 评论/反馈 | 有维护与审核能力 | 论坛已承担主互动；站内评论或第三方组件另行评估 |
| 多语言/新主题 | 出现明确受众需求 | 文案资源与设计 tokens |
| 云端收藏/计算同步 | 本地收藏不够用、需要多设备 | 账号云存储、配额与迁移（依赖阶段 4 服务端） |
| 论坛进阶能力（私信、积分） | 社区规模与审核能力支持 | 独立模块，逐项评估（附件已提前落地，见阶段 4） |

每项按「需求 → 契约 → 实现 → 验证 → 发布」单独交付，不因列入候选就提前安装依赖或创建空模块。

### 已提前落地的部分

- [x] **在线逆向工作台（2026-09-16）**：需求为「在线反编译基岩版 BDS，做一个简易 IDA 前端，后端直接用 IDA MCP 的接口」。
  - 界面：工具箱目录里的入口卡片 → 独立全屏页面 `/ida/`（IDA 式三栏：函数列表 / 伪代码·反汇编 /
    交叉引用·栈变量），函数名搜索、伪代码行内地址可点跳转。
  - 后端：`backend/app/reverse/`（`mcp_client` / `schemas` / `client` / `router`）代理本机 `ida-pro-mcp`
    （HTTP JSON-RPC），不引入 MCP SDK。
  - 权限：**访客与会员只读**，admin/superadmin 可重命名函数与切换实例；权限在服务端判定。
  - 边界：65 个工具中只开放只读白名单 + 少量「沉淀分析成果」类写入；
    `patch` / `py_eval` / `py_exec_file` / `put_int` / `undefine` 等**刻意不开放**。
  - 决策见 [ADR-004](decisions/ADR-004-在线逆向工作台代理IDA-MCP.md)，
    实装与验证证据见 [07-开发日志](07-开发日志-逆向工作台.md)。
  - 验证：新增 29 项后端测试（含权限与白名单断言）全通过、全量 116 项通过、
    `astro check` 0 错误、`npm run build` 9 页；真实 BDS 实例联调（17.1 万函数概览、
    `main` 反编译 432 ms / 反汇编 237 ms）与浏览器端到端（访客只读 / 管理员改名真实生效）均通过。
  - 限制：必须后端与 IDA 同机；管理员之间共享一个实例（后端加锁保证不串数据）；
    读接口对访客开放（用户明确选择），如需收紧给读接口加登录要求即可。

- [x] **`.mcstructure` 解析（2026-09-16）**：需求为「解析基岩版结构文件并提供接口，覆盖完整结构 + 实体 + 方块实体 NBT」。
  - 契约：`GET /api/mcstructure/info`；`POST /api/mcstructure/{parse,voxels,slice,position,blocks}`；
    前端类型由 OpenAPI 生成（`src/ports/api-schema.d.ts`），前端模块 `src/modules/mcstructure/`。
  - 实现：`backend/app/nbt_le.py`（自实现小端 NBT 读取器，纯标准库）+ `backend/app/parsers/mcstructure.py`（语义层）。
    决策与候选库实测结论见 [ADR-003](decisions/ADR-003-基岩版结构文件解析器.md)。
  - 验证：pytest 137 项通过（新增 109 项，含字节序反证、截断、超大声明长度、压缩膨胀、ZYX 下标、
    两层共位、方块实体/实体 NBT 透出、multipart 全链路与错误码）；夹具用第三方 `nbtlib` 独立复读自校验；
    `astro check` 0 error；`npm run build` 通过。
  - **真实文件核对完成（2026-09-16）**：游戏内 Structure Block 导出的
    `sticky-piston-1x1x1.mcstructure` 已入库为回归夹具（`backend/tests/fixtures/real/`）。
    借此发现文档与真实实现的格式偏差——`structure_world_origin` 在真实文件里位于**根层级**，
    而文档写在 `structure` 内部，旧实现会把它**静默读成 `None`**；现已两处都支持，
    并用 `world_origin_source` 标明取值来源。
  - 限制：只读（不提供写回/下载）；这是全站**唯一需要上传文件**的功能，已在 `/info` 与前端模块注明。

## 首版完成定义

必须同时满足：四个导航入口（首页 / 作品 / 工具箱 / 论坛）、完整内容发布路径、真实博客/作品、**有可运行工具**（原写「两项工具」；2026-09-16 工具箱收敛后按实际工具数计）、论坛与账号（至少本地演示版，满足阶段 4 验收）、可扩展接口、移动端/键盘可用、无草稿泄露、构建与核心检查通过、可发布可回退。

只做出漂亮首页、工具只显示卡片、论坛与账号停留在演示版而未记录正式化路径、依赖尚未锁定或没有发布回退记录，都不算正式项目完成。

## 待决策登记

| ID | 事项 | 当前默认方向 | 何时必须定案 |
| --- | --- | --- | --- |
| D01 | 正式头像与文案 | 当前头像仅示意 | 首次正式内容验收前 |
| D02 | 技术栈 | Astro + TypeScript 已随阶段 1 初始化落地（2026-09-16）；复杂工具使用 React 的需求出现时再引入 | 已落地；React 岛按需 |
| D03 | 内容管理 | 本地 Markdown 与结构化清单 | 内容模块实现前 |
| D04 | 首批工具 | 区块坐标、材料清单已于 2026-09-16 **移除**；当前为 `.mcstructure` 编辑器 | 已闭环（按需求收敛） |
| D05 | 托管与域名 | **已定（2026-09-16）**：本机运行 + 樱花 HTTP 隧道 + GoDaddy 解析 + 本地同源入口（ADR-009）；静态内容不做多级降级 | 本机侧已落地；公网侧待面板配置后验收 |
| D06 | 研究代码转产品 | 独立验证后再接入 | 启动相关专项前 |
| D07 | 论坛与账号正式服务方案 | 后端 Python + 数据库 SQLite 已定（ADR-001）；托管形态随 D05 定为「本机常驻进程 + 隧道」 | 已随 D05 定案 |

后续任务描述应引用具体阶段和文档，例如：“实施阶段 1，保持现有 Demo 可访问，暂不发布”。完成后更新任务状态、验收证据和相关 ADR。
