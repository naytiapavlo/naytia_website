-r requirements.txt
pytest>=8
# httpx 已随 requirements.txt 进入运行期依赖（逆向工作台调用 IDA MCP 用，
# 测试里也用它的 MockTransport 假扮 MCP 服务），这里不再重复声明。
# 仅用于「夹具自校验」：tests/fixtures/mcstructure_fixtures.py --check 用 nbtlib
# 独立复读生成的字节，验证夹具本身是合法小端 NBT。被测代码不依赖它。
# PyPI 直连不通时可临时指定镜像安装：
#   pip install -i https://mirrors.aliyun.com/pypi/simple nbtlib
nbtlib>=2.0
